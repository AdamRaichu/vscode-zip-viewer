{
  "name": "zip-viewer",
  "displayName": "Zip Tools",
  "description": "An extension which allows for the manipulation of zip (jar, vsix, etc.) files.",
  "publisher": "adamraichu",
  "icon": "logo-icons.png",
  "license": "MIT",
  "author": {
    "name": "AdamRaichu"
  },
  "categories": [
    "Visualization"
  ],
  "keywords": [
    "zip",
    "extract",
    "jar",
    "preview",
    "viewer"
  ],
  "repository": {
    "url": "https://github.com/AdamRaichu/vscode-zip-viewer"
  },
  "version": "2.2.1",
  "main": "./dist/main.js",
  "browser": "./dist/main.js",
  "activationEvents": [
    "onCommand:zipViewer.extract",
    "onCommand:zipViewer.zip",
    "onCommand:zipViewer.openFileWithEditor",
    "onCustomEditor:zipViewer.ZipEdit"
  ],
  "engines": {
    "vscode": "^1.63.0"
  },
  "contributes": {
    "commands": [
      {
        "command": "zipViewer.extract",
        "title": "Zip Viewer: Extract (unzip) a zip file from workspace",
        "shortTitle": "Extract Zip"
      },
      {
        "command": "zipViewer.zip",
        "title": "Zip Viewer: Create a zip file from folder",
        "shortTitle": "Zip Folder"
      },
      {
        "command": "zipViewer.openFileWighEditor",
        "title": "Zip Viewer: Open a zip file with the custom editor",
        "shortTitle": "Open w/ zip editor",
        "icon": "$(explorer-view-icon)"
      }
    ],
    "customEditors": [
      {
        "viewType": "zipViewer.ZipEdit",
        "displayName": "Zip Viewer",
        "selector": [
          {
            "filenamePattern": "*.zip"
          },
          {
            "filenamePattern": "*.vsix"
          },
          {
            "filenamePattern": "*.mcworld"
          },
          {
            "filenamePattern": "*.mcpack"
          },
          {
            "filenamePattern": "*.mcaddon"
          },
          {
            "filenamePattern": "*.jar"
          }
        ],
        "priority": "default"
      }
    ],
    "configuration": [
      {
        "title": "Zip Extracter",
        "properties": {
          "zipViewer.zipTypes": {
            "type": "array",
            "description": "An array of file extensions to check files against when unzipping.",
            "items": {
              "type": "string",
              "title": "File Extension",
              "pattern": "\\.([A-Za-z0-9\\.])"
            },
            "default": [
              ".zip",
              ".vsix",
              ".mcworld",
              ".mcpack",
              ".mcaddon",
              ".jar"
            ]
          },
          "zipViewer.picky": {
            "type": "boolean",
            "markdownDescription": "Whether the extension should automatically fail operations when file extension is not in `zipTypes`. If set to true, extension will only attempt to unzip files with extensions found in `zipTypes`.",
            "default": true
          },
          "zipViewer.unzippedSuffix": {
            "type": "string",
            "markdownDescription": "The suffix to add to folder name when unzipping a zip file. Must not contain any of the following characters: `/ ? % * : | \" < >`.",
            "default": "_unzipped",
            "pattern": "^[^\\/?%*:|\"<>]+$"
          }
        }
      }
    ]
  },
  "devDependencies": {
    "@types/vscode": "^1.63.0",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "scripts": {
    "build": "webpack"
  }
}